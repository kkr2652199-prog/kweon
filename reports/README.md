# 보고서·산출물 저장 위치

프로젝트 `.cursor/rules/google-drive-sync.mdc` 규약:

- **커서 작업 보고서 (`.md`)** → `D:\MONEY lol\My_Drive_Sync\커서보고서\`
- **풀백 로그·`jsonl` 등 실행 산출물** → 같은 Drive 아래 `커서보고서\3kweon_reports_runtime\` 로 옮김(잠금 중인 파일은 로컬에 남을 수 있음).

소스·DB 작업 루트는 `d:\3kweon` (Drive는 백업·기록용).

이 폴더(`d:\3kweon\reports`)에는 필요 시 안내용 `README.md`와, 다른 프로세스가 붙잡은 로그 등만 남을 수 있다.
